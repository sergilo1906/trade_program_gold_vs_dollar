# TODO: ZIP de Plantillas Mejoradas

No se encontró un ZIP de plantillas mejoradas en el workspace con patrón:

- `(plantill|template|prompt|checklist).*(\.zip)$`

## Ruta esperada

- `./_templates/plantillas_mejoradas.zip`

## Integración recomendada

1. Copiar el ZIP en la ruta esperada dentro del repo.
2. Descomprimir temporalmente fuera del repo (por ejemplo: `$env:TEMP\\xauusd_templates_tmp`).
3. Abrir y seguir primero `README` y/o `CHECKLIST` del paquete.
4. Aplicar los ajustes al proyecto usando esa checklist como referencia.
5. Eliminar la carpeta temporal al finalizar.
